<%= form_with(url: set_current_user_path) do |form| %>
  <%= form.select :user_id, options_from_collection_for_select(User.all, :id, :name, get_current_user.id), {}, onchange: 'submit()', class: "block shadow rounded-md border border-gray-400 outline-none px-3 py-2 mt-2 w-full" %>
<% end %>
